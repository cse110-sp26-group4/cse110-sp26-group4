name: Deploy JSDoc to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true # Allow only one concurrent deployment

jobs:
  build:
    name: Build documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm ci
      - run: npm run build-docs
      - name: Restructure artifact
        run: |
          mkdir -p docs/dist
          cp docs/index.html docs/dist/index.html
          cp docs/styles.css docs/dist/styles.css
          touch docs/dist/.nojekyll
          cp -r docs/assets docs/dist/assets
          cp -r docs/jsdoc docs/dist/jsdoc
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/dist

  deploy:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4